const RestaurantCollapsableMenuNested = ({ data }) => {
  console.log("Nested", data);
  return (
    <div>
      <p>Hello from the collapsable menu</p>
    </div>
  );
};

export default RestaurantCollapsableMenuNested;
